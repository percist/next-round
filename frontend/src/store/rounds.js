import { fetch } from './csrf.js';

const initialState = {}

function reducer(state = initialState, action){
    let newState;
    switch (action.type){
        default:
            return state;
    }
}

export default reducer;